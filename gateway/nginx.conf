server {
  listen 80;

  location /api/ {
    proxy_pass http://backend:8000;
  }

  location /admin/ {
    proxy_pass http://backend:8000;
  }

  location /static/ {
    alias /staticfiles/;
  }

  location / {
    root /frontend;
    index index.html;
    try_files $uri /index.html;
  }
}
